export type { ActivityStatus, Location, TransportDetails, Activity, ItineraryDay, TripEvent, WeatherInfo, TravelEssential, DayProgram, ItineraryData } from '~/lib/types';
// All itinerary-related types are now re-exported from app/lib/types for consistency across the app.
